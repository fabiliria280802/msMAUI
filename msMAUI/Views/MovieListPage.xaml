<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:msMAUI.Models"
             xmlns:viewModels="clr-namespace:msMAUI.ViewModels"
             x:DataType="viewModels:MovieListPageViewModel"
             x:Class="msMAUI.Views.MovieListPage"
             Title="Biblioteca de películas">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="+" Command="{Binding AddUpdateMovieCommand}" />
    </ContentPage.ToolbarItems>
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="TextColor" Value="{AppThemeBinding Light=White, Dark=Black}"/>
            </Style>
            <Style TargetType="Frame">
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=Black, Dark=White}"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ScrollView>
        <StackLayout>
            <VerticalStackLayout>
                <Grid RowDefinitions="*,Auto">
                    <CollectionView ItemsSource="{Binding Movies}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:Movie">
                                <Frame Margin="10" Padding="10">
                                    <StackLayout Margin="10">
                                        <VerticalStackLayout Spacing="15">
                                            <Image x:Name="Imagen"
                                                   Source="{Binding portadaPath}"
                                                   HeightRequest="200"
                                                   WidthRequest="200"
                                                   HorizontalOptions="Center"/>
                                            <Label Text="{Binding title}" FontSize="18" FontAttributes="Bold"/>
                                            <Label Text="{Binding synopsis}" FontSize="15" FontAttributes="Italic"/>
                                        </VerticalStackLayout>
                                    </StackLayout>
                                    <Frame.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={x:RelativeSource AncestorType={x:Type viewModels:MovieListPageViewModel}},Path=DisplayActionCommand}"
                                                              CommandParameter="{Binding .}"/>
                                    </Frame.GestureRecognizers>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Grid>
            </VerticalStackLayout>
        </StackLayout>
    </ScrollView>
</ContentPage>